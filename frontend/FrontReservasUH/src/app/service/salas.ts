export class Salas {

    id: number;
    name: string;
    estado: string;

    constructor(id: number, name: string, estado: string) {
        this.id = id;
        this.name = name;
        this.estado = estado;
    }
}
